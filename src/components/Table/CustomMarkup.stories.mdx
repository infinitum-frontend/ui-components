import { Meta, Unstyled, ArgTypes } from '@storybook/blocks'
import Table from './Table'
import TableBase from 'Components/Table/components/TableBase'

<Meta title="Components/Table/CustomMarkup" />

## Кастомный вариант таблицы

Если в таблицу передается проп `children`, появляется возможность создать легковесную таблицу произвольного формата.
При этом функционал, связанный с сортировкой, фильтрацией, селекцией, изменением размера становится недоступным, а также отпадает необходимость создавать объекты columns и data и передавать их в таблицу.

<Unstyled>
  <Table>
    <thead>
      <tr>
        <th>Заголовок 1</th>
        <th>Заголовок 2</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Ячейка 1</td>
        <td>Ячейка 2</td>
      </tr>
    </tbody>
  </Table>
</Unstyled>

{/* prettier-ignore-start */}
```html
<Table>
  <thead>
    <tr>
      <th>Заголовок 1</th>
      <th>Заголовок 2</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Ячейка 1</td>
      <td>Ячейка 2</td>
    </tr>
  </tbody>
</Table>
```
{/* prettier-ignore-end */}

<ArgTypes of={TableBase} />
